mutation Register($input: AccountRegisterInput!) {
  register(input: $input) {
    account {
      id
      firstName
      lastName
      email
      role
    }
    error {
      ...FieldErrorFragment
    }
  }
}
