import { makeStyles } from "@mui/styles";

export const useSimpleContainerStyles = makeStyles({
  simpleContainer: {
    padding: "1rem",
    boxShadow: "2px 2px 5px rgba(0, 0, 0, 0.25)",
    borderRadius: "0.5rem",
  },
});
