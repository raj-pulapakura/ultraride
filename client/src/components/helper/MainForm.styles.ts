import { makeStyles } from "@mui/styles";

export const useMainFormStyles = makeStyles({
  formTitle: {
    fontSize: "clamp(2rem, 10px, 10rem)",
  },
  formFieldError: {
    marginBottom: "1rem",
  },
});
