import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "Work Sans",
  },
  palette: {
    primary: {
      main: "#6500EA",
    },
    secondary: {
      main: "#D2FFEE",
    },
    background: {
      default: "#fff",
      paper: "#fafafaf",
    },
  },
});
