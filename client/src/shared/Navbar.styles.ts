import { makeStyles } from "@mui/styles";

export const useNavbarStyles = makeStyles({
  navbar: {
    background: "#000",
    padding: "1rem 2rem",
  },
  navbarInner: {},
  brand: {
    color: "white",
  },
});
